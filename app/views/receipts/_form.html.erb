<%= form_with(model: receipt, local: true) do |form| %>
  <% if receipt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(receipt.errors.count, "error") %> prohibited this receipt from being saved:</h2>

      <ul>
      <% receipt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store %>
    <%= form.text_field :store, id: :receipt_store %>
  </div>

  <div class="field">
    <%= form.label :base_amount %>
    <%= form.text_field :base_amount, id: :receipt_base_amount %>
  </div>

  <div class="field">
    <%= form.label :tax_amount %>
    <%= form.text_field :tax_amount, id: :receipt_tax_amount %>
  </div>

  <div class="field">
    <%= form.label :total_amount %>
    <%= form.text_field :total_amount, id: :receipt_total_amount %>
  </div>

  <div class="field">
    <%= form.label :purchased_at %>
    <%= form.datetime_select :purchased_at, id: :receipt_purchased_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
